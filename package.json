{
	"name": "fullstackapp",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "cross-env NODE_ENV=production node index.js",
		"dev": "cross-env NODE_ENV=development nodemon index.js",
		"lint": "eslint .",
		"test": "cross-env NODE_ENV=test jest --verbose --runInBand",
		"build:ui": "rm -rf build && cd ../part1 && npm run build --prod && cp -r build ../fullstackapp",
		"deploy": "git push heroku master",
		"deploy:full": "npm run  buil:ui && git add . && git commit -m uibuild && git push && npm run deploy",
		"logs:prod": "heroku logs --tail"
	},
	"keywords": [],
	"author": "Sam chris",
	"license": "MIT",
	"dependencies": {
		"bcrypt": "^5.0.1",
		"core-js": "^3.10.1",
		"cors": "^2.8.5",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"express-async-errors": "^3.1.1",
		"jsonwebtoken": "^8.5.1",
		"mongoose": "^5.12.3",
		"mongoose-unique-validator": "^2.0.3",
		"morgan": "^1.10.0"
	},
	"devDependencies": {
		"cross-env": "^7.0.3",
		"eslint": "^7.24.0",
		"jest": "^26.6.3",
		"nodemon": "^2.0.7",
		"supertest": "^6.1.3"
	},
	"jest": {
		"testEnvironment": "node"
	}
}
